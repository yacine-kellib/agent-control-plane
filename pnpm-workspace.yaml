packages:
  - 'packages/*'

# The services and the orchestrator moved to the product repository (ACP-66).
# What stays is packages/acp-types, which codegen.sh generates from
# spec/schemas/ -- it IS the wire format, so it belongs with the specification
# rather than with the things that consume it.
